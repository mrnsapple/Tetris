/*
** EPITECH PROJECT, 2018
** djd
** File description:
** dk
*/

#include "list.h"
/*
void	good_lines_and_rows_num(char *file, pieces_t *a)
{
	int     fd;
	int     y = 0;
	char	
	fd = open(file, O_RDONLY, 0644);
	if (fd == -1)
		return (0);
	for (size = read(fd, buff, 1); size > 0;
             size = read(fd, buff, 1)) {
		if (buff[0] == '\n') {
                        y++;
		}

}
*/
